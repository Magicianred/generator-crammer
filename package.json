{
  "name": "generator-crammer",
  "version": "0.1.0",
  "description": "Generates an AngularJS project that uses GulpJS to compile everything into a single HTML file.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "yeoman-generator"
  ],
  "author": "Caden Lovelace",
  "license": "MIT",
  "dependencies": {
    "fleck": "^0.5.1",
    "yeoman-generator": "^0.18.5"
  }
}
